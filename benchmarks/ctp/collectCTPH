for f in */relax.txt; do echo $f; cat $f | grep "Search:" | tr -d "Search:" | python ../average.py; cat $f | grep "Heur:" | tr -d "Heur:" | python ../average.py; cat $f | grep "user" | tr -d "user" | python ../avgtime.py; done
